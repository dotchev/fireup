processes:
  default-dir:
    cmd: node -e "console.log(process.cwd());"

  relative-dir:
    dir: ../unit
    cmd: node -e "console.log(process.cwd());"

  absolute-dir:
    dir: /
    cmd: node -e "console.log(process.cwd());"

processes:
  - name: default-dir
    cmd: node -e "console.log(process.cwd());"

  - name: relative-dir
    dir: ../unit
    cmd: node -e "console.log(process.cwd());"

  - name: absolute-dir
    dir: /
    cmd: node -e "console.log(process.cwd());"
